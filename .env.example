# EduCoder Coding Hub Supabase public frontend variables
# For Vite, use VITE_ prefix. For compatibility, NEXT_PUBLIC_ names are also supported in code.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-publishable-or-anon-key

# Never put service-role keys, database passwords, JWT secrets, or private keys in this frontend repository.
